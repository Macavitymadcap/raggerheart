# RAG-0001: enhancements

**Initial Prompt**

The app works, but it's slow and inaccurate. I have implemented some of the optimisations in the first section of the `optimization.md` file, but there has been little improvement.

I believe that by properly using markdown files as my source documents I'll get better material. To that end I want you to review the parsers, and make sure they are up to scratch. There seems to be an issue with the document parser accessing the wrong methods of pdf parser. Like with the store, they should all implement the same interface to make switching between them easier, and should be wrapped in a parser factory or similar pattern.

The data also needs to be stored in a more efficient format. For that I want to implement qdrant. I shall be running it locally through docker. Implement the qdrant adapter so I can slot it in and use it with this system.

the rag part of this app should be generic, so that i could put any kinds of documents about anything in there, it's only when we come to the ui tht it begins to be geared towards daggerheart.

The ui also needs some work - it's getting messy with it being in template literals. Seeing as I'm using htmx, and server heavy app and bun has jsx out of the box, all server side html should be written with jsx to allow for easy templating and syntax highlighting.

To recap, I want the following improvements:

- Implement the remaining optimisations
- integrate markdown parsing and have the parser stage detect which parser to use for the given documents.
- hook in a qdrant vector store
- rewrite the ui using jsx for server sent html

make any improvements/changes to other files as required to implement these requested items, and tidy up any redundancies. 

I will be following your instructions on my local machine, so show me full files I can swap out, segments I can copy and paste into replace smaller parts, and any commands and installations required.